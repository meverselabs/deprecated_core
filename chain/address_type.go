package chain

import (
	"git.fleta.io/fleta/common"
)

// account address types
const (
	KeyAccountType         = common.AddressType(1)
	MultiSigAccountType    = common.AddressType(2)
	FormulationAccountType = common.AddressType(2)
)
